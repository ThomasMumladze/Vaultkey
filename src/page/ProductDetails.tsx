const ProductDetails = () => {
    const gallery = Array.from({ length: 4 });
    const similarGames = Array.from({ length: 4 });
    const reviews = Array.from({ length: 3 });
    const dlcList = Array.from({ length: 4 });

    return <article className="pages--product-details"></article>;
};

export default ProductDetails;
